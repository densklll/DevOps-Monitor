name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repository layout
        run: |
          test -f docker-compose.yml
          test -f locustfile.py
          test -d k8s
          test -d backend/tests
          test -d ci
          test -d .github/workflows

# 2022-01-23: Note K8s CPU requests for backend (CI runner)

# 2022-02-08: Adjust CI layout verify step scope (staging)

# 2022-02-22: Tighten integration service timeout budget (demo box)

# 2022-03-15: Clarify K8s CPU requests for backend (staging)

# 2022-04-06: Adjust Compose dependency boot order (staging)

# 2022-04-30: Align Redis broker URL for compose networking (CI runner)

# 2022-05-18: Capture CI layout verify step scope (CI runner)

# 2022-05-29: Align Ingress TLS and default certificate (staging)

# 2022-06-17: Stub pytest isolation for pipeline models (prod checklist)

# 2022-07-09: Review K8s CPU requests for backend (local dev)

# 2022-08-02: Review pytest isolation for pipeline models (demo box)

# 2022-08-12: Review CI layout verify step scope (local dev)
